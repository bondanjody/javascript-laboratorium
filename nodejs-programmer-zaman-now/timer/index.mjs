// contoh Timer : setInterval
setInterval(() => {
    console.log(`Program dimulai dari : ${new Date()}`);
},1000)
// setInterval digunakan untuk melakukan infiniteloop (mengeksekusi terus menerus callback selama selang waktu tertentu)
